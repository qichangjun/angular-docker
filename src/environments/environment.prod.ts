export const environment = {
  production: true,
  transferApi : '/transferapi',
  cookieName:'teamwork',
  teamrowkApi:'/teamworkapi',
  adminApi : '/adminapi',
  adminUrl : `http://${window.location.host}/admin/#/login?returnUrl=`,
  teamworkUrl : `http://${window.location.host}/teamwork`
};
